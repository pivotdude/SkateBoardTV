import React from 'react';
import {PreviewVideoModel} from "../../Models";

interface MyVideosProps {
    video: PreviewVideoModel
}

const MyVideo = (props: MyVideosProps) => {
    return (
        <div>
            
        </div>
    );
};

export default MyVideo;